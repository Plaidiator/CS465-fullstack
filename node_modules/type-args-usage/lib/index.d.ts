import { OptionRules } from 'type-args';
export declare const title: (content: string) => string;
export declare const body: (content: string, leadingSpaces?: number, trailingSpaces?: number, lineWidth?: number) => string;
export declare const options: (optionRules: OptionRules, leadingSpaces?: number, lineWidth?: number) => string;
